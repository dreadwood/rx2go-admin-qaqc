include /elements/images

mixin qq-btn-play({ text, className })
  button.qq-btn-play(class=className onclick=clickHandler)
    +svg({ name: "player-play-2", width: 18, height: 18 })
    // svg(width="18" height="18")
      use(xlink:href="/img/sprite.svg#player-pause")
    span=text
